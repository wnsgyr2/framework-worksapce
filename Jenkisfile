pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Verify framework with samples') {
      steps {
        dir('root-project') {
          sh 'mvn -U clean verify'
        }
      }
    }
  }
}
